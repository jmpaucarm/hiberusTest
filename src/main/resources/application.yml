spring:
  application:
    name: payment-initiation-service

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

# Swagger UI y documentación OpenAPI en runtime (SpringDoc)
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
